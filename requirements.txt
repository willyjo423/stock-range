pandas>=2.2
numpy>=1.26
requests>=2.31
# Daily bars, split- and dividend-adjusted, batched. Stooq is the fallback and
# needs nothing but requests.
yfinance>=0.2.40
# read_html on the Wikipedia membership table
lxml>=5.0
# Needed once the model layer lands; harmless now.
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3
pyarrow>=15.0
